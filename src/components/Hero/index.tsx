//resolver container
import { FormForm, HeroTitle } from './styles'

const Hero = () => (
  <FormForm>
    <div className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </FormForm>
)
export default Hero
